<main class="card shadow-xl bg-base-200 sm:w-1/2 m-auto">
  <div class="card-body">
    <header>
      <h1 class="card-title"><%= t(:forgot_password) %></h1>
    </header>
    <div class="h-4"></div>
    <%= form_with(url: identity_password_reset_path) do |form| %>
      <%= form.email_field :email,
        autofocus: true,
        required: true,
        autocomplete: "email",
        placeholder: t(:email),
        class: "simple_form__input w-full"
      %>
      <div class="h-10"></div>
      <%= form.button t(:send_password_reset_email), class: "simple_form__btn w-full" %>
    <% end %>
  </div>
</main>
