<div id="flash" class="toast toast-top toast-center z-50 w-full p-0">
  <% if flash.present? %>
    <% flash.each do |name, msg| %>
      <%= render 'flash_message', locals: { name:, msg:  } %>
    <% end %>
  <% end %>
</div>
