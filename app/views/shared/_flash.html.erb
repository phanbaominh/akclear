<div id="flash" class=" toast toast-top sm:toast-right w-full sm:w-fit p-0 z-50 top-10 h-fit">
  <% if flash.present? %>
    <% flash.each do |name, msg| %>
      <%= render partial: 'shared/flash_message', locals: { name: name, msg: msg  } %>
    <% end %>
  <% end %>
</div>
