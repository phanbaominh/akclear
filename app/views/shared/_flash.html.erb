<div id="flash" class="toast toast-top toast-right z-50 top-10">
  <% if flash.present? %>
    <% flash.each do |name, msg| %>
      <%= render partial: 'shared/flash_message', locals: { name: name, msg: msg  } %>
    <% end %>
  <% end %>
</div>
