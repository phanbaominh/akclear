<%= content_tag(
          :button,
          nil,
          class: "operators__new_operator_btn btn btn-circle btn-primary",
          formmethod: "GET",
          formaction: new_clears_used_operator_path,
          disabled: disabled,
          form: 'clears__form'
        ) do %>
  <%= heroicon "plus", options: { class: "h-4" } %>
<% end %>
